import { getAllDay } from "@/services/DayServices";
import Layout from "@/components/layout/Layout";
import { useCallback } from "react";

const Home = () => {
	return (
		<Layout title="Home Page">
			<div>Home Page: coming soon - click on users or days</div>
		</Layout>
	);
};

export default Home;
